# 合同管理系统配置文件
server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: contract-management
  
  # 数据库配置 - 使用远程 MySQL 数据库
  datasource:
    url: jdbc:mysql://sh-cynosdbmysql-grp-ikhg477i.sql.tencentcdb.com:21589/aofeimysql?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: aofei
    password: NG()G5*zbwLb
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  # JPA配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        
  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      
  # 邮件配置
  mail:
    host: smtp.company.com
    port: 587
    username: system@company.com
    password: password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            
# JWT配置
jwt:
  secret: mySecretKey123456789012345678901234567890
  expiration: 86400000 # 24小时
  
# 文件存储配置
file:
  upload-path: ./uploads/
  
# 日志配置
logging:
  level:
    com.company.contract: DEBUG
    org.springframework.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"